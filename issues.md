# Issues

- The model is using tables that do not display in rendered chat, is it using markdown?

- Should the model be told to limit reponses? Some seem very long and tables are not useful if they are too wide, this is a small chat widget, not wide, and lines that wrap are not great for UX

services:
  api:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - DB_PATH=/data/receptionist.db
    volumes:
      - sqlite_data:/data
    restart: unless-stopped

volumes:
  sqlite_data:
